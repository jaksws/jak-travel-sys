APP_NAME=Laravel # Optional, used for the application name
APP_ENV=local # Optional, used for environment
APP_KEY= # Optional, used for encryption
APP_DEBUG=true # Optional, used for debugging
APP_URL=http://localhost # Optional, used for the application URL
APP_PORT=8000 # Optional, used for the application port
APP_ASSET_URL=http://localhost # Optional, used for asset URL
APP_PATH=/ # Optional, used for the application path
APP_ASSET_PATH=/ # Optional, used for asset path
APP_ASSET_URL=http://localhost # Optional, used for asset URL
APP_ASSET_PREFIX= # Optional, used for asset prefix
APP_ASSET_URL_PATH=/ # Optional, used for Vite
APP_ENV_FILE=.env # Optional, used for the environment file
APP_DEBUG_BAIDU= # Optional, used for Baidu debugging
APP_DEBUG_GITHUB= # Optional, used for GitHub debugging
APP_DEBUG_GITHUB_TOKEN= # Optional, used for GitHub token
APP_DEBUG_GITHUB_REPO= # Optional, used for GitHub repository
APP_DEBUG_GITHUB_REPO_URL= # Optional, used for GitHub repository URL
APP_DEBUG_GITHUB_REPO_NAME= # Optional, used for GitHub repository name
APP_DEBUG_GITHUB_REPO_BRANCH= # Optional, used for GitHub repository branch
APP_DEBUG_GITHUB_REPO_PATH= # Optional, used for GitHub repository path
APP_DEBUG_GITHUB_REPO_COMMIT= # Optional, used for GitHub repository commit
APP_DEBUG_GITHUB_REPO_COMMIT_URL= # Optional, used for GitHub repository commit URL
APP_DEBUG_GITHUB_REPO_COMMIT_NAME= # Optional, used for GitHub repository commit name
APP_DEBUG_GITHUB_REPO_COMMIT_BRANCH= # Optional, used for GitHub repository commit branch
APP_DEBUG_GITHUB_REPO_COMMIT_PATH= # Optional, used for GitHub repository commit path
APP_DEBUG_GITHUB_REPO_COMMIT_MESSAGE= # Optional, used for GitHub repository commit message
APP_DEBUG_GITHUB_REPO_COMMIT_AUTHOR= # Optional, used for GitHub repository commit author
APP_DEBUG_GITHUB_REPO_COMMIT_AUTHOR_EMAIL= # Optional, used for GitHub repository commit author email
APP_DEBUG_GITHUB_REPO_COMMIT_AUTHOR_NAME= # Optional, used for GitHub repository commit author name
APP_DEBUG_GITHUB_REPO_COMMIT_AUTHOR_URL= # Optional, used for GitHub repository commit author URL
APP_TIMEZONE=UTC # Optional, used for Carbon
APP_LOCALE=en # Optional, used for Carbon
APP_FALLBACK_LOCALE=en # Optional, used for Carbon
APP_FAKER_LOCALE=en_US # Optional, used for Faker

APP_MAINTENANCE_DRIVER=file # Optional, used for maintenance mode
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4 # Optional, used for PHP CLI server

BCRYPT_ROUNDS=12 # Optional, used for bcrypt hashing

LOG_CHANNEL=stack # Optional, used for logging
LOG_STACK=single # Optional, used for logging
LOG_DEPRECATIONS_CHANNEL=null # Optional, used for deprecations logging
LOG_LEVEL=debug # Optional, used for logging level

DB_CONNECTION=sqlite # Optional, used for database connection
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database # Optional, used for session storage
SESSION_LIFETIME=120 # Optional, used for session lifetime
SESSION_ENCRYPT=false # Optional, used for session encryption
SESSION_PATH=/ # Optional, used for session path
SESSION_DOMAIN=null # Optional, used for session domain

BROADCAST_CONNECTION=log # Optional, used for broadcasting
FILESYSTEM_DISK=local # Optional, used for filesystem
QUEUE_CONNECTION=database # Optional, used for queue

CACHE_STORE=database # Optional, used for caching
# CACHE_PREFIX= 

MEMCACHED_HOST=127.0.0.1 # Optional, used for Memcached

REDIS_CLIENT=phpredis # Optional, used for Redis client
REDIS_HOST=127.0.0.1 # Optional, used for Redis host
REDIS_PASSWORD=null # Optional, used for Redis password
REDIS_PORT=6379 # Optional, used for Redis port

MAIL_MAILER=log # Optional, used for mail
MAIL_SCHEME=null # Optional, used for mail scheme
MAIL_HOST=127.0.0.1 # Optional, used for mail host
MAIL_PORT=2525 # Optional, used for mail port
MAIL_USERNAME=null # Optional, used for mail username
MAIL_PASSWORD=null # Optional, used for mail password
MAIL_FROM_ADDRESS="hello@example.com" # Optional, used for mail from address
MAIL_FROM_NAME="${APP_NAME}"    # Optional, used for mail from name

AWS_ACCESS_KEY_ID= # Optional, used for AWS access key ID
AWS_SECRET_ACCESS_KEY= # Optional, used for AWS secret access key
AWS_DEFAULT_REGION=us-east-1 # Optional, used for AWS region
AWS_BUCKET= # Optional, used for AWS bucket
AWS_USE_PATH_STYLE_ENDPOINT=false # Optional, used for AWS path style endpoint

VITE_APP_NAME="${APP_NAME}" # Optional, used for Vite app name
